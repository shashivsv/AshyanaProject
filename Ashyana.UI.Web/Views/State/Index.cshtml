@model IEnumerable<Ashyana.UI.Web.ViewModel.MyStateModel>



<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog " style="width:600px;">
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">list </h4>
            </div>
            <div class="modal-body">
                <p>
                    @Html.ActionLink("Create New", "Create")
                </p>
                <table class=" table table-striped table-bordered datatable">
                    <tbody>
                        <tr>
                            <th></th>
                            <th>State</th>
                            <th>Desc</th>
                            <th>Country</th>

                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.Hidden("StateID", @item.StateID)</td>
                                <td>@item.StateName</td>
                                <td>@item.StateDesc</td>
                                <td>@item.CountryName</td>

                                <td>@Html.ActionLink("Edit", "Edit", new { id = item.StateID })</td>
                                <td>@Html.ActionLink("Details", "Details", new { id = item.StateID })</td>
                                <td>@Html.ActionLink("Delete", "Delete", new { id = item.StateID })</td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {

        $('#myModal').modal("show");
    });

</script>